import ClientPortalMUI from '@/app/components/ClientPortalMUI'; 

export const metadata = {
  title: 'Portal del Cliente — AutoLink',
};

export default function PageCliente() {
  return <ClientPortalMUI />;
}
